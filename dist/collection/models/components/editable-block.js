import { get, has, set } from 'lodash-es';
export class EditableBlockConfig {
  constructor(obj) {
    // make DatalistItemData idempotent
    if (obj instanceof EditableBlockConfig) {
      return obj;
    }
    // Add props
    for (const name of Object.keys(obj)) {
      this[name] = obj[name];
    }
  }
  getProp(key) {
    return get(this, key);
  }
  hasProp(key) {
    return has(this, key);
  }
  setProp(key, value) {
    set(this, key, value);
  }
}
export class EditableBlockDataData {
  constructor(obj) {
    // make DatalistItemData idempotent
    if (obj instanceof EditableBlockDataData) {
      return obj;
    }
    // Add props
    for (const name of Object.keys(obj)) {
      this[name] = obj[name];
    }
  }
  getProp(key) {
    return get(this, key);
  }
  hasProp(key) {
    return has(this, key);
  }
  setProp(key, value) {
    set(this, key, value);
  }
}
export class EditableBlockData {
  constructor(obj) {
    // make DatalistItemData idempotent
    if (obj instanceof EditableBlockData) {
      return obj;
    }
    // Add props
    for (const name of Object.keys(obj)) {
      this[name] = obj[name];
    }
  }
  getProp(key) {
    return get(this, key);
  }
  hasProp(key) {
    return has(this, key);
  }
  setProp(key, value) {
    set(this, key, value);
  }
}
