import { get, has, set } from 'lodash-es';
export class AppPeriodicConsumptionItemCaption {
  constructor(obj) {
    if (obj instanceof AppPeriodicConsumptionItemCaption) {
      return obj;
    }
    // Add props
    for (const name of Object.keys(obj)) {
      this[name] = obj[name];
    }
  }
  getProp(key) {
    return get(this, key);
  }
  hasProp(key) {
    return has(this, key);
  }
  setProp(key, value) {
    set(this, key, value);
  }
}
export class AppPeriodicConsumptionItem {
  constructor(obj) {
    if (obj instanceof AppPeriodicConsumptionItem) {
      return obj;
    }
    // Add props
    for (const name of Object.keys(obj)) {
      this[name] = obj[name];
    }
  }
  getProp(key) {
    return get(this, key);
  }
  hasProp(key) {
    return has(this, key);
  }
  setProp(key, value) {
    set(this, key, value);
  }
}
export class AppPeriodicConsumption {
  constructor(obj) {
    if (obj instanceof AppPeriodicConsumption) {
      return obj;
    }
    // Add props
    for (const name of Object.keys(obj)) {
      this[name] = obj[name];
    }
  }
  getProp(key) {
    return get(this, key);
  }
  hasProp(key) {
    return has(this, key);
  }
  setProp(key, value) {
    set(this, key, value);
  }
}
